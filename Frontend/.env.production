# Production Environment Variables for Netlify Deployment
# Note: Backend is not deployed, so API calls will fail gracefully

# Backend API URL - Set this in Netlify UI if you have a backend
# For now, this will cause API calls to fail gracefully with error handling
NEXT_PUBLIC_API_URL=https://api.placeholder.com/api

# Optional: Analytics
# NEXT_PUBLIC_GA_ID=

# Optional: Sentry for error tracking
# NEXT_PUBLIC_SENTRY_DSN=
