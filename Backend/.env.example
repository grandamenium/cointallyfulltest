# Backend Environment Variables

# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/cointally

# Application Port
PORT=3001

# JWT Configuration
JWT_SECRET=your-secret-key-here-minimum-32-characters
JWT_EXPIRATION=7d

# CORS Configuration
# For local: http://localhost:3000
# For production: https://your-frontend.render.com,https://your-domain.com
CORS_ORIGIN=http://localhost:3000

# Redis Configuration (for job queue)
REDIS_HOST=localhost
REDIS_PORT=6379

# Encryption Key (32-byte hex key for AES-256-GCM)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your-32-byte-hex-key-here

# External API Keys
COINGECKO_API_KEY=your-coingecko-api-key
ETHERSCAN_API_KEY=your-etherscan-api-key
HELIUS_API_KEY=your-helius-api-key

# Exchange API Keys (Optional - users provide their own)
# These are for testing purposes only
# COINBASE_API_KEY=
# COINBASE_API_SECRET=
# BINANCE_API_KEY=
# BINANCE_API_SECRET=
# KRAKEN_API_KEY=
# KRAKEN_API_SECRET=

# Email Configuration (Optional)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-specific-password
# SMTP_FROM=noreply@cointally.com

# Environment
NODE_ENV=development